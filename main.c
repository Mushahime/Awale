#include "awale.h"

// Not Relevant

int main()
{
    /*JeuAwale jeu;
    initialiser_plateau(&jeu);

    int joueur = 1;
    while (!verifier_fin_partie(&jeu))
    {
        afficher_plateau(&jeu);
        printf("Joueur %d, choisissez une case (%d-%d): ", joueur, (joueur == 1) ? 0 : 6, (joueur == 1) ? 5 : 11);
        int case_depart;
        scanf("%d", &case_depart);

        while (!jouer_coup(&jeu, joueur, case_depart))
        {
            printf("Coup invalide. Reessayez.\n");
            printf("Joueur %d, choisissez une case (%d-%d): ", joueur, (joueur == 1) ? 0 : 6, (joueur == 1) ? 5 : 11);
            scanf("%d", &case_depart);
            // Vous pouvez ajouter du code ici pour obtenir une nouvelle entrée de l'utilisateur si nécessaire
        }

        joueur = (joueur == 1) ? 2 : 1;
    }

    afficher_plateau(&jeu);
    printf("Partie terminee!\n");
    if (jeu.score_joueur1 > jeu.score_joueur2)
    {
        printf("Joueur 1 gagne!\n");
    }
    else if (jeu.score_joueur2 > jeu.score_joueur1)
    {
        printf("Joueur 2 gagne!\n");
    }
    else
    {
        printf("Match nul!\n");
    }*/

    return 0;
}